#pragma once

#include <vector>
#include <GL\glew.h>
#include <glm.hpp>

namespace fruitjuice {
	using Indices = std::vector<const GLuint>;
	using Vertices = std::vector<const GLfloat>;
	using Normals = std::vector<const GLfloat>;
}
